import { createContext } from "react";

// export const UserContext = createContext({user: "Juan Perez", email: "juanperez@mail.com"});


//en el componente consumidor
// const usuario = useContext(UserContext);
